/**
 * Token `$inject` de la feature `dropdown` — el string bajo el que `NgbDropdownConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-dropdown.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_DROPDOWN_CONFIG = "ngb.dropdown.config";
