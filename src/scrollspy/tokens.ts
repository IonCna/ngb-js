/**
 * Token `$inject` de la feature `scrollspy` — el string bajo el que `NgbScrollSpyConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-scrollspy.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_SCROLLSPY_CONFIG = "ngb.scrollspy.config";
