/**
 * Token `$inject` de la feature `typeahead` — el string bajo el que `NgbTypeaheadConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-typeahead.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_TYPEAHEAD_CONFIG = "ngb.typeahead.config";
