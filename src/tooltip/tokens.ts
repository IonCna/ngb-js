/**
 * Token `$inject` de la feature `tooltip` — el string bajo el que `NgbTooltipConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-tooltip.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_TOOLTIP_CONFIG = "ngb.tooltip.config";
