/**
 * `ngb-js` (root) — solo lo **global**. Cada feature se importa por su propio
 * subpath (`ngb-js/alert`, `ngb-js/modal`, …).
 */
export { NgbModule } from "@ngb/ngb.module";
export { NgbConfig } from "@ngb/config/ngb-config";
