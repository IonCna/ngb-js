/**
 * Token `$inject` de la feature `carousel` — el string bajo el que `NgbCarouselConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-carousel.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_CAROUSEL_CONFIG = "ngb.carousel.config";
