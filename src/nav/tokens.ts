/**
 * Token `$inject` de la feature `nav` — el string bajo el que `NgbNavConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-nav.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_NAV_CONFIG = "ngb.nav.config";
