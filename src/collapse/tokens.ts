/**
 * Token `$inject` de la feature `collapse` — el string bajo el que `NgbCollapseConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-collapse.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_COLLAPSE_CONFIG = "ngb.collapse.config";
