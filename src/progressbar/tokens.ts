/**
 * Token `$inject` de la feature `progressbar` — el string bajo el que `NgbProgressbarConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-progressbar.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_PROGRESSBAR_CONFIG = "ngb.progressbar.config";
