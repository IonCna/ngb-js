/**
 * Token `$inject` de la feature `popover` — el string bajo el que `NgbPopoverConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-popover.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_POPOVER_CONFIG = "ngb.popover.config";
