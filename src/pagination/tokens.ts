/**
 * Token `$inject` de la feature `pagination` — el string bajo el que `NgbPaginationConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-pagination.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_PAGINATION_CONFIG = "ngb.pagination.config";
