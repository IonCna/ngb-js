/**
 * Token `$inject` de la feature `rating` — el string bajo el que `NgbRatingConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-rating.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_RATING_CONFIG = "ngb.rating.config";
