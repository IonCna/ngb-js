/**
 * Token `$inject` de la feature `accordion` — el string bajo el que `NgbAccordionConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-accordion.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_ACCORDION_CONFIG = "ngb.accordion.config";
