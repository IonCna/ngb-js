/**
 * Token `$inject` de la feature `toast` — el string bajo el que `NgbToastConfig`
 * queda registrado en el `$injector` de AngularJS (`ngb-toast.module.ts` lo
 * provee con `useFactory`). Distinto del `$name` interno del `@Service`.
 */
export const NGB_TOAST_CONFIG = "ngb.toast.config";
